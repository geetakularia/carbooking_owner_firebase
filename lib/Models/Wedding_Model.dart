class WeddingModel {
  String img, title, subtitle, ammount, discount;
  WeddingModel(
      {required this.title,
      required this.ammount,
      required this.discount,
      required this.img,
      required this.subtitle});
}
